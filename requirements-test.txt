# VELMA - Dependencias de Testing
# Instalar con: pip install -r requirements-test.txt

# Dependencias core de VELMA (necesarias para que los tests corran)
-r requirements.txt

# Framework de testing
pytest>=7.4.0
pytest-timeout>=2.2.0      # Timeout por test (evita tests colgados)

# Cobertura de código
pytest-cov>=4.1.0          # Reporte de coverage

# Property-based testing (opcional pero recomendado)
hypothesis>=6.82.0         # Tests con inputs generados automáticamente

# Utilidades
numpy>=1.24.0              # Ya en requirements.txt, pero lo explicitamos
